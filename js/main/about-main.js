"use strict";

angular.module('app-counterdraft')
    .controller('aboutController', ['$http', '$timeout', 'counterDraft-message-srv', function($http, $timeout, messageService) {
        var self = this;

    }]);
